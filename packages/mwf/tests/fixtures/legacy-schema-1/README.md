# Legacy schema 1 fixture

Captured from the retired Python runtime before removal on 2026-09-09.
The project files and recall.json are golden compatibility evidence, not TS-generated expectations.
Recall query: embedded objects; path: reports/a.docx; file_type: .docx; operation: conversion.
Keep these inputs fixed when changing the current runtime. The old AGENTS block is intentional test data.

Trailing Markdown whitespace in the generated index was normalized for repository checks.
