interface:
  display_name: "Memory With Files"
  short_description: "Project-local memory across agent sessions"
  default_prompt: "Use $memory-with-files to initialize or maintain this project's .mwf memory."

policy:
  allow_implicit_invocation: true
